[package]
name = "copetch"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
image = { version = "0.25", default-features = false, features = ["png", "gif"] }

[profile.release]
opt-level = 3
strip = true
lto = true
codegen-units = 1